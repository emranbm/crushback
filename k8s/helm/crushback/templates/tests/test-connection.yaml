apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crushback.fullname" . }}-test-connection"
  labels:
    {{- include "crushback.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crushback.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
