apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "crushback.fullname" . }}-backend-route
spec:
  host: api.crushback.net
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: crushback-backend
    weight: 100
  wildcardPolicy: None
