apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prometheus.config.configMapName" . }}
  labels:
    {{- include "prometheus.labels" . | nindent 4 }}
data:
  prometheus.yml:
    {{- tpl .Values.prometheusConfig . | toYaml | indent 4 }}
